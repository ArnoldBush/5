function linear_search($array, $target) {
    foreach ($array as $index => $value) {
        if ($value === $target) {
            return $index;
        }
    }
    return -1;
}

$numbers = [1, 2, 3, 4, 5];
$target = 3;
$result = linear_search($numbers, $target);
echo $result;
